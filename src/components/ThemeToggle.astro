---
import { getUiText } from '../ui';

const tr = getUiText();
---

<button
  type="button"
  class="theme-toggle"
  id="theme-toggle"
  aria-label={tr.themeToggle}
  aria-pressed="false"
  title={tr.themeToggle}
  data-label-to-light={tr.themeToLight}
  data-label-to-dark={tr.themeToDark}
>
  <svg
    class="theme-icon"
    xmlns="http://www.w3.org/2000/svg"
    width="19"
    height="19"
    viewBox="0 0 20 20"
    aria-hidden="true"
  >
    <circle class="theme-icon-ring" cx="10" cy="10" r="6.75"></circle>
    <path class="theme-icon-phase" d="M10 3.25a6.75 6.75 0 0 1 0 13.5 3.65 6.75 0 0 0 0-13.5Z"
    ></path>
  </svg>
</button>

<script is:inline>
  (function () {
    function mountThemeToggle() {
      const root = document.documentElement;
      const btn = document.getElementById('theme-toggle');
      if (!(btn instanceof HTMLButtonElement) || btn.dataset.themeReady === 'true') return;
      btn.dataset.themeReady = 'true';

      const labelToLight = btn.dataset.labelToLight || 'Switch to light mode';
      const labelToDark = btn.dataset.labelToDark || 'Switch to dark mode';
      const syncAriaPressed = (dark) => {
        btn.setAttribute('aria-pressed', String(dark));
        btn.setAttribute('aria-label', dark ? labelToLight : labelToDark);
      };

      syncAriaPressed(root.getAttribute('data-theme') === 'dark');

      const observer = new MutationObserver(() => {
        syncAriaPressed(root.getAttribute('data-theme') === 'dark');
      });
      observer.observe(root, { attributes: true, attributeFilter: ['data-theme'] });

      btn.addEventListener('click', () => {
        const next = root.getAttribute('data-theme') === 'dark' ? 'light' : 'dark';
        root.setAttribute('data-theme', next);
        try {
          localStorage.setItem('theme', next);
        } catch {
          // Keep the visible toggle working when storage is unavailable.
        }
        syncAriaPressed(next === 'dark');
      });
    }

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', mountThemeToggle, { once: true });
    } else {
      mountThemeToggle();
    }
  })();
</script>

<style>
.theme-toggle {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  /* Visual size stays 2rem; hit area expands to the 44px WCAG 2.5.5 minimum. */
  width: 2rem;
  height: 2rem;
  min-width: 44px;
  min-height: 44px;
  /* Negative margin keeps layout spacing while the padding grows the tap target. */
  margin: -6px;
  border: none;
  background: transparent;
  cursor: pointer;
  color: var(--label-secondary);
  transition:
    color var(--duration-fast) var(--ease-out),
    opacity var(--duration-fast) var(--ease-out);
  flex-shrink: 0;
}

.theme-toggle:hover {
  color: var(--label-primary);
}

.theme-toggle:focus-visible {
  outline: var(--focus-ring-width) solid var(--focus-ring-color);
  outline-offset: 2px;
  border-radius: var(--radius-sm);
}

.theme-icon {
  display: block;
  overflow: visible;
  transition: transform var(--duration-normal) var(--ease-out);
}

.theme-icon-ring {
  fill: none;
  stroke: currentColor;
  stroke-width: 1.45;
}

.theme-icon-phase {
  fill: currentColor;
  opacity: 0.62;
  transform-origin: center;
  transition: opacity var(--duration-fast) var(--ease-out);
}

:global([data-theme='dark']) .theme-icon {
  transform: rotate(180deg);
}

:global([data-theme='dark']) .theme-icon-phase {
  opacity: 0.76;
}
</style>
